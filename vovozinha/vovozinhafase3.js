function escolha () {
    var opcoes = prompt ("1 - Abrir o guarda-roupa enquanto o Lobo está de costas e matá-lo. \n2 - Ficar escondida no guarda-roupa e esperar por ajuda.");
    if (opcoes == 1) {
        return location = "vovozinhavenceu.html"
    } else if (opcoes == 2) {
        return location = "vovozinhagameover3.html"
    } else {
    alert ('Opção inválida')
    return escolha ();
    
    }
    }